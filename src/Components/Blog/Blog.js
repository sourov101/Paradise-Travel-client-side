import React from 'react';
import { Helmet } from 'react-helmet';

const Blog = () => {
    return (
        <div>
            <Helmet>
                <title>Paradise Travel: Blog</title>
            </Helmet>
        </div>
    );
};

export default Blog;